import React from 'react';

import { BrowserRouter, Switch } from 'react-router-dom';

const Routes = () => (
  <BrowserRouter>
    <Switch>
      { /* Import and add your Route components here */ }
    </Switch>
  </BrowserRouter>
);

export default Routes;
